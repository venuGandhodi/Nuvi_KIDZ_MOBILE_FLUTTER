// Default Flutter widget test — scaffold removed.
// See test/features/ and test/core/ for the NUVI KIDZ test suite.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — see test/features/ for NUVI KIDZ tests', () {
    expect(true, isTrue);
  });
}
